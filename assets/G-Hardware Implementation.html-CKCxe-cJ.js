import{_ as l,c as n,b as a,d,e as s,a as i,w as t,r as c,o as h,f as o}from"./app-BC3GIMfQ.js";const p={},u={class:"table-of-contents"};function m(g,e){const r=c("router-link");return h(),n("div",null,[e[13]||(e[13]=a("h1",{id:"第7章-✅",tabindex:"-1"},[a("a",{class:"header-anchor",href:"#第7章-✅"},[a("span",null,"-第7章[✅]")])],-1)),e[14]||(e[14]=a("p",null,"-第7章",-1)),d(" more "),a("nav",u,[a("ul",null,[a("li",null,[i(r,{to:"#简单介绍主要是基础"},{default:t(()=>e[0]||(e[0]=[o("简单介绍主要是基础")])),_:1,__:[0]})]),a("li",null,[i(r,{to:"#chapter-7-hardware-implementation"},{default:t(()=>e[1]||(e[1]=[o("Chapter 7. Hardware Implementation")])),_:1,__:[1]})]),a("li",null,[i(r,{to:"#_7-1-simt-architecture"},{default:t(()=>e[2]||(e[2]=[o("7.1. SIMT Architecture")])),_:1,__:[2]})]),a("li",null,[i(r,{to:"#_7-2-hardware-multithreading"},{default:t(()=>e[3]||(e[3]=[o("7.2. Hardware Multithreading")])),_:1,__:[3]})]),a("li",null,[i(r,{to:"#_7-✅-hardware-implementation"},{default:t(()=>e[4]||(e[4]=[o("7_✅ Hardware Implementation")])),_:1,__:[4]}),a("ul",null,[a("li",null,[i(r,{to:"#文档原文"},{default:t(()=>e[5]||(e[5]=[o("文档原文")])),_:1,__:[5]})]),a("li",null,[i(r,{to:"#详细解释"},{default:t(()=>e[6]||(e[6]=[o("详细解释")])),_:1,__:[6]})])])]),a("li",null,[i(r,{to:"#_7-1-✅-simt-architecture"},{default:t(()=>e[7]||(e[7]=[o("7.1_✅ SIMT Architecture")])),_:1,__:[7]}),a("ul",null,[a("li",null,[i(r,{to:"#文档原文-1"},{default:t(()=>e[8]||(e[8]=[o("文档原文")])),_:1,__:[8]})]),a("li",null,[i(r,{to:"#详细解释-1"},{default:t(()=>e[9]||(e[9]=[o("详细解释")])),_:1,__:[9]})])])]),a("li",null,[i(r,{to:"#_7-2-✅-hardware-multithreading"},{default:t(()=>e[10]||(e[10]=[o("7.2_✅ Hardware Multithreading")])),_:1,__:[10]}),a("ul",null,[a("li",null,[i(r,{to:"#文档原文-2"},{default:t(()=>e[11]||(e[11]=[o("文档原文")])),_:1,__:[11]})]),a("li",null,[i(r,{to:"#详细解释-2"},{default:t(()=>e[12]||(e[12]=[o("详细解释")])),_:1,__:[12]})])])])])]),e[15]||(e[15]=s('<h2 id="简单介绍主要是基础" tabindex="-1"><a class="header-anchor" href="#简单介绍主要是基础"><span>简单介绍主要是基础</span></a></h2><div class="hint-container info"><p class="hint-container-title">说明</p><p>主要是各种搜索找的学习；</p><p>主题：CUDA核心GPU编程</p><p>前置条件：</p><ul><li>应具备C++编程知识</li><li>需理解内存管理，如malloc和free</li><li>理解STL及其模板机制</li><li>需配置NVIDIA显卡，型号需为900系列或更高</li><li>所用扩展要求版本为11或更新</li><li>编译器版本不低于11</li><li>CMake版本需在3.18以上</li></ul></div><h2 id="chapter-7-hardware-implementation" tabindex="-1"><a class="header-anchor" href="#chapter-7-hardware-implementation"><span>Chapter 7. Hardware Implementation</span></a></h2><ul><li><p>Warning: This document has been replaced by a new CUDA Programming Guide. The information in this document should be considered legacy, and this document is no longer being updated as of CUDA 13.0. Please refer to the CUDA Programming Guide for up- to- date information on CUDA.</p><ul><li>警告：本文档已被新的 CUDA 编程指南取代。本文档中的信息应视为遗留信息，且自 CUDA 13.0 起不再更新。请参阅 CUDA 编程指南以获取最新的 CUDA 信息。</li></ul></li><li><p>The NVIDIA GPU architecture is built around a scalable array of multithreaded Streaming Multiprocessors (SMs). When a CUDA program on the host CPU invokes a kernel grid, the blocks of the grid are enumerated and distributed to multiprocessors with available execution capacity. The threads of a thread block execute concurrently on one multiprocessor, and multiple thread blocks can execute concurrently on one multiprocessor. As thread blocks terminate, new blocks are launched on the vacated multiprocessors.</p><ul><li>NVIDIA GPU 架构构建于可扩展的多线程流式多处理器（SM）阵列之上。当主机 CPU 上的 CUDA 程序调用内核网格时，网格中的块会被枚举并分发到具有可用执行能力的多处理器上。一个线程块中的线程在单个多处理器上并发执行，并且多个线程块可以在一个多处理器上并发执行。随着线程块终止，新的块会在空出来的多处理器上启动。</li></ul></li><li><p>A multiprocessor is designed to execute hundreds of threads concurrently. To manage such a large number of threads, it employs a unique architecture called SIMT (Single- Instruction, Multiple- Thread) that is described in SIMT Architecture. The instructions are pipelined, leveraging instruction- level parallelism within a single thread, as well as extensive thread- level parallelism through simultaneous hardware multithreading as detailed in Hardware Multithreading. Unlike CPU cores, they are issued in order and there is no branch prediction or speculative execution.</p><ul><li>多处理器旨在同时执行数百个线程。为了管理如此大量的线程，它采用了一种称为 SIMT（单指令多线程）的独特架构，该架构在 SIMT 架构章节中描述。指令采用流水线方式，利用单线程内的指令级并行性，以及通过硬件多线程（详见硬件多线程章节）实现的广泛线程级并行性。与 CPU 核心不同，这些指令按顺序发出，没有分支预测或推测执行。</li></ul></li><li><p>SIMT Architecture and Hardware Multithreading describe the architecture features of the streaming multiprocessor that are common to all devices. Compute Capability 5. x, Compute Capability 6. x, and Compute Capability 7. x provide the specifics for devices of compute capabilities 5. x, 6. x, and 7. x respectively.</p><ul><li>SIMT 架构和硬件多线程描述了所有设备通用的流式多处理器架构特性。计算能力 5.x、计算能力 6.x 和计算能力 7.x 分别提供了计算能力为 5.x、6.x 和 7.x 的设备的具体细节。</li></ul></li><li><p>The NVIDIA GPU architecture uses a little- endian representation.</p><ul><li>NVIDIA GPU 架构使用小端表示。</li></ul></li></ul><h2 id="_7-1-simt-architecture" tabindex="-1"><a class="header-anchor" href="#_7-1-simt-architecture"><span>7.1. SIMT Architecture</span></a></h2><ul><li><p>The multiprocessor creates, manages, schedules, and executes threads in groups of 32 parallel threads called warps. Individual threads composing a warp start together at the same program address, but they have their own instruction address counter and register state and are therefore free to branch and execute independently. The term warp originates from weaving, the first parallel thread technology. A half- warp is either the first or second half of a warp. A quarter- warp is either the first, second, third, or fourth quarter of a warp.</p><ul><li>多处理器以 32 个并行线程为一组（称为 warp）来创建、管理、调度和执行线程。组成一个 warp 的各个线程从相同的程序地址一起开始，但它们有自己的指令地址计数器和寄存器状态，因此可以自由地分支并独立执行。warp 这一术语源自编织（weaving），即第一种并行线程技术。半 warp 是 warp 的前半部分或后半部分。四分之一 warp 是 warp 的第一、第二、第三或第四部分。</li></ul></li><li><p>When a multiprocessor is given one or more thread blocks to execute, it partitions them into warps and each warp gets scheduled by a warp scheduler for execution. The way a block is partitioned into warps is always the same; each warp contains threads of consecutive, increasing thread IDs with the first warp containing thread 0. Thread Hierarchy describes how thread IDs relate to thread indices in the block.</p><ul><li>当多处理器获得一个或多个要执行的线程块时，它会将它们划分为 warp，每个 warp 由 warp 调度器调度执行。块划分成 warp 的方式始终相同：每个 warp 包含连续递增线程 ID 的线程，第一个 warp 包含线程 0。线程层次结构描述了线程 ID 如何与块中的线程索引相关联。</li></ul></li><li><p>A warp executes one common instruction at a time, so full efficiency is realized when all 32 threads of a warp agree on their execution path. If threads of a warp diverge via a data- dependent conditional branch, the warp executes each branch path taken, disabling threads that are not on that path. Branch divergence occurs only within a warp; different warps execute independently regardless of whether they are executing common or disjoint code paths.</p><ul><li>一个 warp 一次执行一条公共指令，因此当 warp 中的所有 32 个线程在执行路径上一致时，可实现完全效率。如果 warp 中的线程通过数据相关的条件分支发生发散，则 warp 会执行所采用的每个分支路径，并禁用不在该路径上的线程。分支发散仅发生在 warp 内部；不同的 warp 独立执行，无论它们执行的是公共代码路径还是不相交的代码路径。</li></ul></li><li><p>The SIMT architecture is akin to SIMD (Single Instruction, Multiple Data) vector organizations in that a single instruction controls multiple processing elements. A key difference is that SIMD vector organizations expose the SIMD width to the software, whereas SIMT instructions specify the execution and branching behavior of a single thread. In contrast with SIMD vector machines, SIMT enables programmers to write thread- level parallel code for independent, scalar threads, as well as data- parallel code for coordinated threads. For the purposes of correctness, the programmer can essentially ignore the SIMT behavior; however, substantial performance improvements can be realized by taking care that the code seldom requires threads in a warp to diverge. In practice, this is analogous to the role of cache lines in traditional code: Cache line size can be safely ignored when designing for correctness but must be considered in the code structure when designing for peak performance. Vector architectures, on the other hand, require the software to coalesce loads into vectors and manage divergence manually.</p><ul><li>SIMT 架构类似于 SIMD（单指令多数据）向量组织，因为单条指令控制多个处理元素。一个关键区别在于 SIMD 向量组织向软件公开 SIMD 宽度，而 SIMT 指令指定单个线程的执行和分支行为。与 SIMD 向量机相比，SIMT 使程序员能够为独立的标量线程编写线程级并行代码，也能为协调的线程编写数据并行代码。就正确性而言，程序员基本上可以忽略 SIMT 行为；然而，通过注意使代码中 warp 内的线程很少需要发散，可以实现显著的性能提升。在实践中，这类似于传统代码中缓存行的作用：在为正确性设计时可以安全地忽略缓存行大小，但在为峰值性能设计代码结构时则必须考虑缓存行大小。另一方面，向量架构要求软件将加载合并成向量并手动管理发散。</li></ul></li><li><p>Prior to NVIDIA Volta, warps used a single program counter shared amongst all 32 threads in the warp together with an active mask specifying the active threads of the warp. As a result, threads from the same warp in divergent regions or different states of execution cannot signal each other or exchange data, and algorithms requiring fine- grained sharing of data guarded by locks or mutexes can easily lead to deadlock, depending on which warp the contending threads come from.</p><ul><li>在 NVIDIA Volta 之前，warp 使用一个在所有 32 个线程之间共享的程序计数器，以及一个指定 warp 中活动线程的活动掩码。因此，来自同一 warp 但处于发散区域或不同执行状态的线程无法相互发送信号或交换数据，而需要由锁或互斥量保护的细粒度数据共享的算法很容易导致死锁，具体取决于竞争线程来自哪个 warp。</li></ul></li><li><p>Starting with the NVIDIA Volta architecture, Independent Thread Scheduling allows full concurrency between threads, regardless of warp. With Independent Thread Scheduling, the GPU maintains execution state per thread, including a program counter and call stack, and can yield execution at a per- thread granularity, either to make better use of execution resources or to allow one thread to wait for data to be produced by another. A schedule optimizer determines how to group active threads from the same warp together into SIMT units. This retains the high throughput of SIMT execution as in prior NVIDIA GPUs, but with much more flexibility: threads can now diverge and reconverge at sub- warp granularity.</p><ul><li>从 NVIDIA Volta 架构开始，独立线程调度允许线程之间的完全并发，无论 warp 如何。借助独立线程调度，GPU 会为每个线程维护执行状态（包括程序计数器和调用栈），并可以按线程粒度让出执行权，以便更好地利用执行资源，或允许一个线程等待另一个线程产生数据。调度优化器决定如何将同一 warp 中的活动线程分组到 SIMT 单元中。这保持了与先前 NVIDIA GPU 中相同的 SIMT 执行高吞吐量，但具有更大的灵活性：线程现在可以在子 warp 粒度上发散和再汇聚。</li></ul></li><li><p>Independent Thread Scheduling can lead to a rather different set of threads participating in the executed code than intended if the developer made assumptions about warp- synchronicity of previous hardware architectures. In particular, any warp- synchronous code (such as synchronization- free, intra- warp reductions) should be revisited to ensure compatibility with NVIDIA Volta and beyond. See Compute Capability 7. x for further details.</p><ul><li>如果开发人员对先前硬件架构的 warp 同步性做出了假设，独立线程调度可能导致参与执行代码的线程集合与预期大不相同。特别是，任何 warp 同步代码（例如无需同步的 warp 内规约）都应重新审视，以确保与 NVIDIA Volta 及更高版本的兼容性。详见计算能力 7.x。</li></ul></li></ul><h4 id="note" tabindex="-1"><a class="header-anchor" href="#note"><span>Note:</span></a></h4><ul><li><p>The threads of a warp that are participating in the current instruction are called the active threads, whereas threads not on the current instruction are inactive (disabled). Threads can be inactive for a variety of reasons including having exited earlier than other threads of their warp, having taken a different branch path than the branch path currently executed by the warp, or being the last threads of a block whose number of threads is not a multiple of the warp size.</p><ul><li>参与当前指令的 warp 线程称为活动线程，而未在当前指令上的线程为非活动（禁用）线程。线程可能因多种原因变为非活动状态，包括比其 warp 中的其他线程更早退出、采用了与 warp 当前执行的分支路径不同的分支路径，或者是线程数不是 warp 大小倍数的块中的最后几个线程。</li></ul></li><li><p>If a non- atomic instruction executed by a warp writes to the same location in global or shared memory for more than one of the threads of the warp, the number of serialized writes that occur to that location varies depending on the compute capability of the device (see Compute Capability 5. x, Compute Capability 6. x, and Compute Capability 7. x), and which thread performs the final write is undefined.</p><ul><li>如果 warp 执行的非原子指令对该 warp 中的多个线程写入全局内存或共享内存中的同一位置，则发生到该位置的串行写入次数取决于设备的计算能力（参见计算能力 5.x、计算能力 6.x 和计算能力 7.x），并且最终由哪个线程执行写入是未定义的。</li></ul></li><li><p>If an atomic instruction executed by a warp reads, modifies, and writes to the same location in global memory for more than one of the threads of the warp, each read/modify/write to that location occurs and they are all serialized, but the order in which they occur is undefined.</p><ul><li>如果 warp 执行的原子指令对该 warp 中的多个线程读取、修改和写入全局内存中的同一位置，则每次读/修改/写操作都会发生，并且它们全部被串行化，但是它们发生的顺序是未定义的。</li></ul></li></ul><h2 id="_7-2-hardware-multithreading" tabindex="-1"><a class="header-anchor" href="#_7-2-hardware-multithreading"><span>7.2. Hardware Multithreading</span></a></h2><ul><li><p>The execution context (program counters, registers, and so on) for each warp processed by a multiprocessor is maintained on- chip during the entire lifetime of the warp. Therefore, switching from one execution context to another has no cost, and at every instruction issue time, a warp scheduler selects a warp that has threads ready to execute its next instruction (the active threads of the warp) and issues the instruction to those threads.</p><ul><li>多处理器处理的每个 warp 的执行上下文（程序计数器、寄存器等）在 warp 的整个生命周期内都保存在芯片上。因此，从一个执行上下文切换到另一个执行上下文没有开销，并且在每个指令发出时刻，warp 调度器会选择一个有线程准备好执行下一条指令（该 warp 的活动线程）的 warp，并将指令发给这些线程。</li></ul></li><li><p>In particular, each multiprocessor has a set of 32- bit registers that are partitioned among the warps, and a parallel data cache or shared memory that is partitioned among the thread blocks.</p><ul><li>具体来说，每个多处理器都有一组 32 位寄存器，这些寄存器在 warp 之间划分，还有一个并行数据缓存或共享内存在线程块之间划分。</li></ul></li><li><p>The number of blocks and warps that can reside and be processed together on the multiprocessor for a given kernel depends on the amount of registers and shared memory used by the kernel and the amount of registers and shared memory available on the multiprocessor. There are also a maximum number of resident blocks and a maximum number of resident warps per multiprocessor. These limits as well the amount of registers and shared memory available on the multiprocessor are a function of the compute capability of the device and are given in Compute Capabilities. If there are not enough registers or shared memory available per multiprocessor to process at least one block, the kernel will fail to launch.</p><ul><li>对于给定的内核，可以在多处理器上共同驻留和处理的块和 warp 的数量取决于内核使用的寄存器和共享内存量以及多处理器上可用的寄存器和共享内存量。每个多处理器还有最大驻留块数和最大驻留 warp 数。这些限制以及多处理器上可用的寄存器和共享内存量是设备计算能力的函数，并在计算能力章节中给出。如果每个多处理器没有足够的寄存器或共享内存来处理至少一个块，则内核将无法启动。</li></ul></li><li><p>The total number of warps in a block is as follows:</p><ul><li>块中的 warp 总数如下：</li></ul></li></ul><p>[<br> \\mathrm{ceil}\\left(\\frac{T}{W_{size}},1\\right)<br> ]</p><ul><li><p>T is the number of threads per block, Wsize is the warp size, which is equal to 32, ceil(x, y) is equal to x rounded up to the nearest multiple of y.</p><ul><li>T 是每个块的线程数，Wsize 是 warp 大小，等于 32，ceil(x, y) 等于将 x 向上舍入到 y 的最近整数倍。</li></ul></li><li><p>The total number of registers and total amount of shared memory allocated for a block are documented in the CUDA Occupancy Calculator provided in the CUDA Toolkit.</p><ul><li>为块分配的寄存器总数和共享内存总量在 CUDA 工具包提供的 CUDA 占用率计算器中记录。</li></ul></li></ul><hr><h2 id="_7-✅-hardware-implementation" tabindex="-1"><a class="header-anchor" href="#_7-✅-hardware-implementation"><span>7_✅ Hardware Implementation</span></a></h2><h3 id="文档原文" tabindex="-1"><a class="header-anchor" href="#文档原文"><span>文档原文</span></a></h3><ul><li>NVIDIA GPU 架构是围绕着可扩展的多线程流式多处理器（Streaming Multiprocessors，简称 <code>SMs</code>）阵列构建的。</li><li>当主机 CPU 上的 CUDA 程序调用内核网格（<code>kernel grid</code>）时，网格中的块（<code>blocks</code>）会被枚举并分发到具有可用执行能力的多处理器上。</li><li>一个线程块（<code>thread block</code>）的线程在一个多处理器上并发执行，并且多个线程块可以在一个多处理器上并发执行。</li><li>随着线程块终止，新的块会在腾出的多处理器上启动。</li><li>一个多处理器被设计为并发执行数百个线程。</li><li>为了管理如此大量的线程，它采用了一种称为 <code>SIMT</code>（单指令多线程，Single-Instruction, Multiple-Thread）的独特架构，这将在 <code>SIMT Architecture</code> 中描述。</li><li>指令是流水线化的，利用单线程内的指令级并行性，以及通过 <code>Hardware Multithreading</code> 中详述的同时硬件多线程（simultaneous hardware multithreading）实现的广泛线程级并行性。</li><li>与 CPU 核心不同，它们是按顺序发射（issued in order）的，并且没有分支预测或推测性执行。</li><li><code>SIMT Architecture</code> 和 <code>Hardware Multithreading</code> 描述了所有设备通用的流式多处理器的架构特性。</li><li><code>Compute Capability 5.x</code>、<code>Compute Capability 6.x</code> 和 <code>Compute Capability 7.x</code> 分别提供了计算能力 5.x、6.x 和 7.x 设备的具体细节。</li><li>NVIDIA GPU 架构使用小端（little-endian）表示法。</li></ul><hr><h3 id="详细解释" tabindex="-1"><a class="header-anchor" href="#详细解释"><span>详细解释</span></a></h3><p>这里是理解 GPU 宏观工作原理的入口。不要把 GPU 想象成一个巨大的单一处理器，而要把它想象成一个拥有众多高度专业化车间的大型工厂。</p><ul><li><h4 id="_1-sm-工厂的核心车间" tabindex="-1"><a class="header-anchor" href="#_1-sm-工厂的核心车间"><span>1. SM：工厂的核心车间</span></a></h4><p>NVIDIA GPU 的核心构建模块是 <strong>流式多处理器 (<code>SM</code>)</strong>。如果把 GPU 比作一个巨大的工厂，<code>SM</code> 就是工厂里的独立车间。</p><ul><li><strong>可扩展性 (Scalability):</strong> 这是一个关键设计。无论你买的是入门级显卡还是顶级的计算卡，区别主要在于这种“车间”的数量。你的 CUDA 代码（内核）不需要知道硬件有多少个 <code>SM</code>，驱动程序会自动把任务（<code>blocks</code>）分配给空闲的车间。这就是为什么同一段代码能在不同型号的 GPU 上运行，只是快慢不同。</li></ul></li><li><h4 id="_2-任务分配机制" tabindex="-1"><a class="header-anchor" href="#_2-任务分配机制"><span>2. 任务分配机制</span></a></h4><ul><li><strong>Grid 与 Block 的调度:</strong> 当你发射一个 Kernel 时，你实际上是创建了一个 <code>Grid</code>，里面包含了成千上万个 <code>Block</code>。GPU 的硬件调度器（GigaThread Engine）负责把这些 <code>Block</code> 像发牌一样分发给各个 <code>SM</code>。</li><li><strong>驻留 (Residency):</strong> 一旦一个 <code>Block</code> 被分配给某个 <code>SM</code>，它就会一直驻留在该 <code>SM</code> 上直到执行结束。它不会中途“搬家”到另一个 <code>SM</code>。</li></ul></li><li><h4 id="_3-cpu-vs-gpu-设计哲学的本质差异" tabindex="-1"><a class="header-anchor" href="#_3-cpu-vs-gpu-设计哲学的本质差异"><span>3. CPU vs. GPU 设计哲学的本质差异</span></a></h4><p>文档中提到 <code>SM</code> 没有“分支预测”或“推测性执行”，这是理解 GPU 性能特性的关键：</p><ul><li><strong>CPU (法拉利):</strong> 设计目标是<strong>低延迟</strong>。为了让单线程跑得快，CPU 塞满了复杂的逻辑：乱序执行（Out-of-Order）、分支预测（猜你下一步要跑什么）。这就像赛车，为了快哪怕浪费油（晶体管/功耗）也值得。</li><li><strong>GPU (公交车队):</strong> 设计目标是<strong>高吞吐量</strong>。GPU 去掉了复杂的预测逻辑，把省下来的晶体管全部用来堆砌运算单元（ALU）。它不在乎单个线程跑得是否极快，而在乎能不能同时运送海量的数据。</li><li><strong>代价:</strong> 因为没有分支预测，如果你的代码里有大量的 <code>if-else</code> 跳转，GPU 的流水线可能会因为等待而停顿，这就是为什么我们常说 GPU 讨厌复杂的控制流。</li></ul></li></ul><hr><h2 id="_7-1-✅-simt-architecture" tabindex="-1"><a class="header-anchor" href="#_7-1-✅-simt-architecture"><span>7.1_✅ SIMT Architecture</span></a></h2><h3 id="文档原文-1" tabindex="-1"><a class="header-anchor" href="#文档原文-1"><span>文档原文</span></a></h3><ul><li>多处理器以 32 个并行线程为一组来创建、管理、调度和执行线程，这一组线程称为束（<code>warps</code>）。</li><li>组成一个 <code>warp</code> 的各个线程在同一个程序地址一起开始执行，但它们拥有自己的指令地址计数器和寄存器状态，因此可以自由地分支并独立执行。</li><li>术语 <code>warp</code> 源自编织（weaving），这是最早的并行线程技术。</li><li>半束（<code>half-warp</code>）是指 <code>warp</code> 的前半部分或后半部分。四分一束（<code>quarter-warp</code>）是指 <code>warp</code> 的第一、第二、第三或第四个四分之一部分。</li><li>当一个多处理器被分配一个或多个线程块执行时，它将它们划分为 <code>warps</code>，并且每个 <code>warp</code> 由 <code>warp</code> 调度器调度执行。</li><li>块划分为 <code>warps</code> 的方式总是相同的；每个 <code>warp</code> 包含具有连续、递增线程 ID 的线程，第一个 <code>warp</code> 包含线程 0。<code>Thread Hierarchy</code> 描述了线程 ID 如何与块内的线程索引相关联。</li><li>一个 <code>warp</code> 一次执行一条公共指令，因此当一个 <code>warp</code> 的所有 32 个线程在执行路径上达成一致时，效率最高。</li><li>如果一个 <code>warp</code> 的线程通过依赖数据的条件分支发生发散（diverge），<code>warp</code> 会执行每一个被采取的分支路径，同时禁用（disable）不在该路径上的线程。</li><li>分支发散仅发生在 <code>warp</code> 内部；不同的 <code>warps</code> 独立执行，无论它们执行的是相同还是不相交的代码路径。</li><li><code>SIMT</code> 架构类似于 <code>SIMD</code>（单指令多数据）向量组织，即单条指令控制多个处理元素。</li><li>一个关键区别是 <code>SIMD</code> 向量组织向软件暴露了 <code>SIMD</code> 宽度，而 <code>SIMT</code> 指令指定单个线程的执行和分支行为。</li><li>与 <code>SIMD</code> 向量机相比，<code>SIMT</code> 允许程序员为独立的标量线程编写线程级并行代码，以及为协调线程编写数据并行代码。</li><li>为了正确性，程序员本质上可以忽略 <code>SIMT</code> 行为；然而，通过确代码很少要求 <code>warp</code> 中的线程发散，可以实现显著的性能提升。</li><li>在实践中，这类似于传统代码中缓存行（cache lines）的角色：在设计正确性时可以安全地忽略缓存行大小，但在设计峰值性能的代码结构时必须考虑它。</li><li>另一方面，向量架构要求软件手动将加载合并为向量并管理发散。</li><li>在 NVIDIA Volta 之前，<code>warps</code> 使用一个在所有 32 个线程之间共享的程序计数器（PC），以及一个指定 <code>warp</code> 中活跃线程的活跃掩码（active mask）。</li><li>结果是，来自同一个 <code>warp</code> 的处于发散区域或不同执行状态的线程无法相互发送信号或交换数据，且需要由锁或互斥锁保护的细粒度数据共享的算法很容易导致死锁，这取决于竞争线程来自哪个 <code>warp</code>。</li><li>从 NVIDIA Volta 架构开始，独立线程调度（Independent Thread Scheduling）允许线程之间完全并发，无论它们是否在同一个 <code>warp</code> 中。</li><li>通过独立线程调度，GPU 维护每个线程的执行状态，包括程序计数器和调用栈，并且可以在每线程粒度上让出（yield）执行，以更好地利用执行资源或允许一个线程等待另一个线程生成数据。</li><li>调度优化器决定如何将来自同一 <code>warp</code> 的活跃线程分组到 <code>SIMT</code> 单元中。这保留了像以前 NVIDIA GPU 一样的 <code>SIMT</code> 执行的高吞吐量，但具有更大的灵活性：线程现在可以在子束（sub-warp）粒度上发散和重新收敛。</li><li>如果开发者对以前硬件架构的 <code>warp</code> 同步性（warp-synchronicity）做出了假设，独立线程调度可能会导致参与执行代码的线程集合与预期的截然不同。</li><li>特别是，任何 <code>warp</code> 同步代码（例如无同步的、<code>warp</code> 内归约）都应重新审视，以确保与 NVIDIA Volta 及更高版本的兼容性。有关更多详细信息，请参阅 <code>Compute Capability 7.x</code>。</li><li>注意：参与当前指令的 <code>warp</code> 线程称为活跃线程（active threads），而不参与当前指令的线程称为非活跃线程（inactive/disabled）。</li><li>线程可能由于多种原因处于非活跃状态，包括比其 <code>warp</code> 的其他线程更早退出、采取了与 <code>warp</code> 当前执行的分支路径不同的分支路径，或者是一个线程数不是 <code>warp</code> 大小倍数的块中的最后几个线程。</li><li>如果一个 <code>warp</code> 执行的非原子指令写入全局或共享内存中的同一位置，并且涉及 <code>warp</code> 中的多个线程，则对该位置发生的序列化写入次数取决于设备的计算能力（参见 <code>Compute Capability 5.x</code>、<code>Compute Capability 6.x</code> 和 <code>Compute Capability 7.x</code>），并且哪个线程执行最后一次写入是未定义的。</li><li>如果一个 <code>warp</code> 执行的原子指令读取、修改并写入全局内存中的同一位置，并且涉及 <code>warp</code> 中的多个线程，则对该位置的每次读取/修改/写入都会发生并且它们都是序列化的，但它们发生的顺序是未定义的。</li></ul><hr><h3 id="详细解释-1" tabindex="-1"><a class="header-anchor" href="#详细解释-1"><span>详细解释</span></a></h3><p>这部分是 CUDA 编程中最核心、也是最容易被误解的概念。让我们拆解 <code>SIMT</code> 的奥秘。</p><ul><li><h4 id="_1-warp-最小的执行单位" tabindex="-1"><a class="header-anchor" href="#_1-warp-最小的执行单位"><span>1. Warp：最小的执行单位</span></a></h4><ul><li>虽然我们在写 CUDA 代码时是按“线程” (<code>thread</code>) 思考的，但硬件并不单独执行一个线程。硬件的执行单位是 <strong>Warp（束）</strong>。</li><li><strong>类比：</strong> 想象一个 32 人的舞蹈团（<code>Warp</code>）。虽然每个人（<code>Thread</code>）都是独立的舞者，但他们必须听从同一个指挥（指令单元）。指挥喊“向前跳”，32 个人必须同时向前跳。</li><li><strong>Warp 的构成:</strong> 一个 <code>Warp</code> 始终包含 32 个线程。这些线程是基于 ID 连续划分的（线程 0-31 是第一个 Warp，32-63 是第二个，依此类推）。</li></ul></li><li><h4 id="_2-分支发散-branch-divergence-simt-的代价" tabindex="-1"><a class="header-anchor" href="#_2-分支发散-branch-divergence-simt-的代价"><span>2. 分支发散 (Branch Divergence)：SIMT 的代价</span></a></h4><p>这是 CUDA 性能优化的重中之重。</p><ul><li><strong>问题:</strong> 如果在这个 32 人的舞蹈团里，指挥说：“穿红鞋的人（<code>if</code> 条件）向左跳，穿蓝鞋的人（<code>else</code> 条件）向右跳”，会发生什么？</li><li><strong>硬件行为:</strong> 这种架构无法让一半人向左跳的同时另一半人向右跳。 <ol><li>硬件会先让“穿红鞋”的人向左跳，此时“穿蓝鞋”的人必须<strong>原地冻结</strong>（被 Mask 掉，处于 inactive 状态）。</li><li>等红鞋组跳完后，硬件再让“穿蓝鞋”的人向右跳，此时“穿红鞋”的人冻结。</li><li>最后大家重新汇合。</li></ol></li><li><strong>后果:</strong> 执行时间变成了两个分支路径时间之和。这就是<strong>分支发散</strong>。</li><li><strong>专家建议:</strong> 尽量让同一个 <code>Warp</code> 内的线程走相同的逻辑路径。</li></ul></li><li><h4 id="_3-simt-vs-simd-标量的错觉" tabindex="-1"><a class="header-anchor" href="#_3-simt-vs-simd-标量的错觉"><span>3. SIMT vs. SIMD：标量的错觉</span></a></h4><p>文档中做了一个精彩的对比。</p><ul><li><strong>SIMD (如 CPU 的 AVX 指令):</strong> 你必须显式地处理向量。比如你必须告诉 CPU “把这 8 个数字加到那 8 个数字上”。这很繁琐，且难以处理 <code>if-else</code>。</li><li><strong>SIMT (CUDA):</strong> NVIDIA 提供了一种“标量错觉”。你只需要写 <code>C = A + B</code>（针对单个线程），硬件会自动把 32 个线程的指令打包成类似向量的操作。</li><li><strong>优势:</strong> 这使得并行编程变得容易得多（你可以写普通的 <code>if</code> 语句），但在性能调优时，你必须透过这种抽象看到底层的 <code>Warp</code> 行为。</li></ul></li><li><h4 id="_4-volta-架构的革命-独立线程调度-its" tabindex="-1"><a class="header-anchor" href="#_4-volta-架构的革命-独立线程调度-its"><span>4. Volta 架构的革命：独立线程调度 (ITS)</span></a></h4><p>这是一个分水岭式的改变（Compute Capability 7.0+）。</p><ul><li><strong>Pre-Volta (Pascal 及以前):</strong> 整个 <code>Warp</code> 共享一个程序计数器 (PC)。如果线程 A 和 B 在同一个 Warp 且进入了不同的分支，它们被硬件强行“锁死”在一起，必须串行执行，绝对无法在这个过程中互相通信（容易死锁）。</li><li><strong>Volta &amp; Beyond:</strong> 每个线程现在拥有自己独立的 PC 和调用栈！ <ul><li><strong>意义:</strong> 虽然为了效率它们通常仍然一起运行，但硬件现在允许它们在更细的粒度上“分头行动”并重新汇合。这解决了许多旧架构下的死锁问题（例如，线程 A 等待线程 B 的锁，但在旧架构下线程 B 因为线程 A 在运行而被硬件挂起，导致死锁）。</li></ul></li><li><strong>警示:</strong> 以前很多“聪明”的黑客代码假设“同一个 Warp 内的线程默认是同步的”（implicit warp synchronization）。这种假设在 Volta 之后不再安全！必须使用 <code>__syncwarp()</code> 等显式同步指令。</li></ul></li><li><h4 id="_5-内存竞争与原子性" tabindex="-1"><a class="header-anchor" href="#_5-内存竞争与原子性"><span>5. 内存竞争与原子性</span></a></h4><ul><li><strong>非原子写 (Race Condition):</strong> 如果 Warp 里 32 个线程同时写同一个内存地址，<strong>谁赢是未定义的</strong>。这就像 32 个人同时向一张纸上写字，最后留下的字迹是谁的完全看运气。</li><li><strong>原子写 (Atomic):</strong> 使用原子指令（如 <code>atomicAdd</code>），硬件保证 32 次操作都会发生，且排队执行（序列化）。虽然顺序未定义，但总结果是正确的。</li></ul></li></ul><hr><h2 id="_7-2-✅-hardware-multithreading" tabindex="-1"><a class="header-anchor" href="#_7-2-✅-hardware-multithreading"><span>7.2_✅ Hardware Multithreading</span></a></h2><h3 id="文档原文-2" tabindex="-1"><a class="header-anchor" href="#文档原文-2"><span>文档原文</span></a></h3><ul><li>每个多处理器处理的每个 <code>warp</code> 的执行上下文（程序计数器、寄存器等）在 <code>warp</code> 的整个生命周期内都在芯片上维护。</li><li>因此，从一个执行上下文切换到另一个没有任何成本，并且在每个指令发射时间，<code>warp</code> 调度器都会选择一个有线程准备好执行其下一条指令的 <code>warp</code>（<code>warp</code> 的活跃线程），并将该指令发射给这些线程。</li><li>特别是，每个多处理器都有一组 32 位寄存器，这些寄存器在 <code>warps</code> 之间进行分区，以及在线程块之间分区的并行数据缓存或共享内存（<code>shared memory</code>）。</li><li>对于给定的内核，可以驻留并在多处理器上一起处理的块和 <code>warp</code> 的数量取决于内核使用的寄存器和共享内存的数量，以及多处理器上可用的寄存器和共享内存的数量。</li><li>每个多处理器还有最大驻留块数和最大驻留 <code>warp</code> 数的限制。这些限制以及多处理器上可用的寄存器和共享内存的数量是设备计算能力的函数，并在 <code>Compute Capabilities</code> 中给出。</li><li>如果每个多处理器没有足够的寄存器或共享内存来处理至少一个块，则内核将无法启动。</li><li>一个块中的 <code>warp</code> 总数如下： <ul><li><code>ceil(T / Wsize, 1)</code></li><li>T 是每个块的线程数，</li><li><code>Wsize</code> 是 <code>warp</code> 大小，等于 32，</li><li><code>ceil(x, y)</code> 等于 x 除以 y 向上取整到最接近的 y 的倍数。</li></ul></li><li>为一个块分配的寄存器总数和共享内存总量记录在 CUDA Toolkit 提供的 <code>CUDA Occupancy Calculator</code> 中。</li></ul><hr><h3 id="详细解释-2" tabindex="-1"><a class="header-anchor" href="#详细解释-2"><span>详细解释</span></a></h3><p>这部分解释了 GPU 如何通过“大量线程”来掩盖“内存延迟”。这是 GPU 高性能的秘诀。</p><ul><li><h4 id="_1-零成本上下文切换-zero-cost-context-switching" tabindex="-1"><a class="header-anchor" href="#_1-零成本上下文切换-zero-cost-context-switching"><span>1. 零成本上下文切换 (Zero-Cost Context Switching)</span></a></h4><ul><li><strong>CPU 的痛点:</strong> 在 CPU 上切换线程很昂贵。操作系统需要把当前寄存器状态保存到内存（RAM），再从内存加载下一个线程的状态。这需要时间。</li><li><strong>GPU 的魔法:</strong> GPU 的 <code>SM</code> 拥有巨大的寄存器文件（Register File）。所有活跃 <code>Warp</code> 的状态（上下文）<strong>始终</strong>存储在芯片上的寄存器中。</li><li><strong>切换过程:</strong> 调度器想切换 <code>Warp</code> 时，只需要把“指针”指向另一组寄存器即可。这个过程消耗 <strong>0 个时钟周期</strong>。</li></ul></li><li><h4 id="_2-延迟掩盖-latency-hiding" tabindex="-1"><a class="header-anchor" href="#_2-延迟掩盖-latency-hiding"><span>2. 延迟掩盖 (Latency Hiding)</span></a></h4><p>这是理解 GPU 为什么需要成千上万个线程的关键。</p><ul><li><strong>场景:</strong> 假设 Warp A 执行了一条读取显存的指令（<code>Load</code>）。显存非常慢，数据返回可能需要几百个时钟周期。</li><li><strong>处理:</strong> Warp A 此时进入“等待”状态。在这个间隙，调度器立刻（0 成本）切换到 Warp B 执行计算。当 Warp B 也卡住等待数据时，切换到 Warp C。</li><li><strong>结果:</strong> 只要有足够多的 Warp（足够的<strong>占用率 Occupancy</strong>），<code>SM</code> 的计算单元就永远不会闲着。</li><li><strong>类比:</strong> 把 <code>SM</code> 想象成一个只有一个灶台的厨师。如果他只做一个菜（一个 Warp），等水烧开（内存读取）的时候他就没事干了。但他如果同时做 20 个菜，这口锅在烧水时，他立刻去切另一个菜的菜。只要菜够多，厨师的手就永远不停。</li></ul></li><li><h4 id="_3-资源限制与占用率-occupancy" tabindex="-1"><a class="header-anchor" href="#_3-资源限制与占用率-occupancy"><span>3. 资源限制与占用率 (Occupancy)</span></a></h4><p>既然线程越多越好，为什么不无限多呢？因为资源有限。</p><ul><li><strong>寄存器压力:</strong> 每个线程都要消耗寄存器。如果你的 Kernel 写得很复杂，每个线程用了太多寄存器，<code>SM</code> 上能同时驻留的 Warp 数量就会减少。</li><li><strong>共享内存压力:</strong> 同样，Shared Memory 是被 Block 占用的。如果每个 Block 用的太多，能驻留的 Block 就变少。</li><li><strong>木桶效应:</strong> 寄存器、共享内存、以及硬件的最大 Warp 数限制，这三者中任何一个达到瓶颈，都会限制 <code>SM</code> 上的并发 Warp 数量（即限制了 Occupancy）。</li><li><strong>计算公式:</strong> 这里的 <code>ceil(T / 32)</code> 告诉我们，即使你只申请了 33 个线程，硬件也会分配 2 个 Warp（64 个线程的资源），因为 Warp 是最小分配单位。</li></ul></li></ul>',36))])}const f=l(p,[["render",m]]),b=JSON.parse('{"path":"/article/CUDA/C-CUDAc-Programming-Guide/G-Hardware%20Implementation.html","title":"-第7章[✅]","lang":"zh-CN","frontmatter":{"date":"2025-08-31T00:00:00.000Z","category":["C++","CUDA开启的GPU编程"],"tag":["C++","CUDA开启的GPU编程"],"feed":false,"seo":false,"head":[]},"headers":[{"level":2,"title":"简单介绍主要是基础","slug":"简单介绍主要是基础","link":"#简单介绍主要是基础","children":[]},{"level":2,"title":"Chapter 7. Hardware Implementation","slug":"chapter-7-hardware-implementation","link":"#chapter-7-hardware-implementation","children":[]},{"level":2,"title":"7.1. SIMT Architecture","slug":"_7-1-simt-architecture","link":"#_7-1-simt-architecture","children":[]},{"level":2,"title":"7.2. Hardware Multithreading","slug":"_7-2-hardware-multithreading","link":"#_7-2-hardware-multithreading","children":[]},{"level":2,"title":"7_✅ Hardware Implementation","slug":"_7-✅-hardware-implementation","link":"#_7-✅-hardware-implementation","children":[{"level":3,"title":"文档原文","slug":"文档原文","link":"#文档原文","children":[]},{"level":3,"title":"详细解释","slug":"详细解释","link":"#详细解释","children":[]}]},{"level":2,"title":"7.1_✅ SIMT Architecture","slug":"_7-1-✅-simt-architecture","link":"#_7-1-✅-simt-architecture","children":[{"level":3,"title":"文档原文","slug":"文档原文-1","link":"#文档原文-1","children":[]},{"level":3,"title":"详细解释","slug":"详细解释-1","link":"#详细解释-1","children":[]}]},{"level":2,"title":"7.2_✅ Hardware Multithreading","slug":"_7-2-✅-hardware-multithreading","link":"#_7-2-✅-hardware-multithreading","children":[{"level":3,"title":"文档原文","slug":"文档原文-2","link":"#文档原文-2","children":[]},{"level":3,"title":"详细解释","slug":"详细解释-2","link":"#详细解释-2","children":[]}]}],"git":{"createdTime":1787540590000,"updatedTime":1787540590000,"contributors":[{"name":"z-ben","username":"z-ben","email":"12800262+z-ben@user.noreply.gitee.com","commits":1,"url":"https://github.com/z-ben"}]},"readingTime":{"minutes":26.86,"words":8059},"filePathRelative":"article/CUDA/C-CUDAc-Programming-Guide/G-Hardware Implementation.md","localizedDate":"2025年8月31日"}');export{f as comp,b as data};
